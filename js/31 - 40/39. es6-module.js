
// es6 module = an external file than contains reusable code that can be
// imported into other js files write reusable code to be use to different codes

import { useState } from "./39. Util.js";



let [count, setCount] = useState();
setCount(count++)
setCount(count++)
setCount(count++)


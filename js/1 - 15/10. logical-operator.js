// logical operator - used to combine or manipulate boolean (true / false)


//           AND  = &&
//           OR = ||
//           NOT = !


const temp = 200;

if(temp > 0 && temp <= 30){
    console.log("the weather if good");
}
else{
    console.log("the weather is bad");
}
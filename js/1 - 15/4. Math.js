// Math is an object that provides a set of methods and properties on using math

// console.log(Math.PI)
// console.log(Math.E)


let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x) //round the number
// z = Math.floor(x) //always round down
// z = Math.ceil(x) //always round up
// z = Math.trunc(x) //removes decimal
// z = Math.pow(x,y) //raise on a given power
// z = Math.sqrt(x) //finds the square root of a number
// z = Math.log(x) //finds the logarithmic number
// z = Math.sin(x)
// z = Math.cos(x)
// z = Math.abs(x) //finds the absolute value
// z = Math.sign(x) //returns -1 if negative negative number and 1 if positive and 0 if 0
// let max = Math.max(x,y,z) //finds the largest number
// let min = Math.min(x,y,z) //finds the lowest number

// let random = Math.floor(Math.random() * 6) + 1 use this if you want to have a random number

console.log(random)
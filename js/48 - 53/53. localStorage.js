// localstorage = is an object to store , read, add ,modify and delete data

localStorage.setItem("data", "some data") //key, value (needs to be a string)
console.log(localStorage.getItem("data")); // get data
localStorage.removeItem("data") //remove date from storage
localStorage.clear() //remove all key value pair
